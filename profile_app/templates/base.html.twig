<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Giedrius. S.{% endblock %}</title>

        {% block stylesheets %}
            {{ importmap('app') }}
        {% endblock %}
    </head>
    <body data-bs-theme="dark">
        <div class="container">
            <nav class="navbar navbar-expand-lg bg-body-tertiary">
                <div class="container-fluid">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get("_route") == "home" %}active{% endif %}" aria-current="page" href="{{ path('home') }}">About</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Projects
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="nav-link {% if app.request.get('_route') == 'weather_home' %}active{% endif %}" href="{{ path('weather_home') }}">Weather API</a></li>
                                </ul>
                            </li>

                        </ul>
                    </div>
                </div>
            </nav>

            {% block body %}
            {% endblock %}
        </div>

        {% block javascripts %}
        {% endblock %}
    </body>
</html>
